import { ConnectButton } from 'opconnect';

function App() {
  return (
    <div
      style={{
        display: 'flex',
        alignItems: 'center',
        justifyContent: 'center',
        height: '100vh',
      }}
    >
      <ConnectButton />
    </div>
  );
}

export default App;
